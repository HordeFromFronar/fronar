//checkEvent(eventId)
eventId = argument[0];
switch(eventId){
    case 0:
        //Abandoned Shack
        //Do any sort of checks on the game state to see if this is a legal event
        return true;
        break;
}
